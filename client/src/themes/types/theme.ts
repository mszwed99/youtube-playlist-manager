import { ThemeColors, ThemeFonts, ThemeRadii, ThemeSpacing } from "styled";

export interface ThemeInterface {
  spacing: ThemeSpacing,
  font: ThemeFonts,
  colors: ThemeColors,
  radii: ThemeRadii,
}